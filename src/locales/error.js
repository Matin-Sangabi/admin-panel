export default Object.freeze({
	themeContext: 'theme context must be use inside ThemeContext',
	themeDispatchContext: 'theme Dispatch context must be use inside ThemeDispatch',
	AuthContext: 'Auth context must be use inside AuthContext',
})
